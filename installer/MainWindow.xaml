<Window x:Class="UYALauncherSetup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UYA Launcher Setup" 
        Height="400" 
        Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#202020">
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="UYA Launcher Setup" 
                   FontSize="24" 
                   FontWeight="Bold"
                   Foreground="White"/>

        <!-- Description -->
        <TextBlock Grid.Row="2" 
                   Text="This will install UYA Launcher with a bundled PCSX2 emulator."
                   FontSize="14"
                   Foreground="#CCCCCC"
                   TextWrapping="Wrap"/>

        <!-- Install Location -->
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" 
                       Text="Install Location:" 
                       FontSize="14"
                       Foreground="White"/>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="InstallPathTextBox"
                         Grid.Column="0"
                         Height="32"
                         VerticalContentAlignment="Center"
                         Padding="8,0"
                         FontSize="13"
                         Background="#2D2D2D"
                         Foreground="White"
                         BorderBrush="#3D3D3D"/>

                <Button x:Name="BrowseButton"
                        Grid.Column="2"
                        Content="Browse..."
                        Width="100"
                        Height="32"
                        Background="#0078D4"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="13"
                        Cursor="Hand"
                        Click="Browse_Click"/>
            </Grid>
        </Grid>

        <!-- Desktop Shortcut Checkbox -->
        <CheckBox x:Name="DesktopShortcutCheckBox"
                  Grid.Row="5"
                  Content="Create desktop shortcut"
                  IsChecked="True"
                  Foreground="White"
                  FontSize="13"
                  Margin="0,0,0,5"/>
        
        <!-- Run After Install Checkbox -->
        <CheckBox x:Name="RunAfterInstallCheckBox"
                  Grid.Row="5"
                  Content="Run UYA Launcher after install"
                  IsChecked="True"
                  Foreground="White"
                  FontSize="13"
                  Margin="0,25,0,0"/>

        <!-- Progress -->
        <StackPanel x:Name="ProgressPanel" 
                    Grid.Row="6" 
                    Visibility="Collapsed"
                    VerticalAlignment="Center">
            <TextBlock x:Name="ProgressText"
                       Text="Installing..."
                       FontSize="14"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>
            <ProgressBar x:Name="ProgressBar"
                         Height="24"
                         Background="#2D2D2D"
                         Foreground="#0078D4"
                         BorderBrush="#3D3D3D"/>
        </StackPanel>

        <!-- Buttons -->
        <Grid Grid.Row="7" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="CancelButton"
                    Grid.Column="2"
                    Content="Cancel"
                    Width="100"
                    Height="36"
                    Background="#3D3D3D"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="13"
                    Cursor="Hand"
                    Click="Cancel_Click"/>

            <Button x:Name="InstallButton"
                    Grid.Column="4"
                    Content="Install"
                    Width="100"
                    Height="36"
                    Background="#0078D4"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Cursor="Hand"
                    Click="Install_Click"/>
        </Grid>
    </Grid>
</Window>
